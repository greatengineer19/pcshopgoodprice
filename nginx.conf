events {}
http {
    upstream backend {
        server backend:8000;  # Matches service name in compose
    }
    server {
        listen 80;
        location / {
            proxy_pass http://backend;
            proxy_set_header Host $host;
        }
    }
}